#!/bin/bash

# Something odd seem to happen when disconnecting/reconnecting laptop to Wifi.
# Afer some investigation it seems it's something to do with Docker/Parallels.
#
# Note this script hasn't been tried yet so tweaking probably required


