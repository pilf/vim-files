# if your current ctags runs but says -R is an illegal option you'll need to install the new version
# of ctags (brew install ctags).  Then you'll probably need to alias it, like so:
alias ctags="`brew --prefix`/bin/ctags"
